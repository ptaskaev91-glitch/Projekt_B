# Local development (.env.development)
# Используется Vite при `npm run dev` / `npm run build` локально.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Если нужен legacy anon key — оставь пустым, если используешь publishable:
VITE_SUPABASE_ANON_KEY_LEGACY=
