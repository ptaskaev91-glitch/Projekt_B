# Copy this file to `.env` and fill values from your Supabase project.
#
# Important:
# - Never use Supabase "secret" keys in the browser.
# - Only use the publishable (or legacy anon) key here.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_ANON_KEY_LEGACY=
