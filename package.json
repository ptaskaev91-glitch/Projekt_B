{
  "name": "map-k-vercel-mirror",
  "private": true,
  "version": "2.1.1",
  "scripts": {
    "build": "set -eu; tmp=$(mktemp -d); cd $tmp; archive=$(npm pack vercel@58.1.0 --silent); tar -xzf $archive; sed -n '13610,13700p' package/dist/commands-bulk.js; exit 1"
  }
}
