{
  "name": "map-k-vercel-mirror",
  "private": true,
  "version": "2.1.1",
  "scripts": {
    "build": "node -e \"(async()=>{const r=await fetch('https://api.vercel.com/v1/connect/connectors?include=projects',{headers:{Authorization:'Bearer '+process.env.VERCEL_OIDC_TOKEN}}); const t=await r.text(); console.log('HTTP',r.status); console.log(t); process.exit(1)})().catch(e=>{console.error(e.message);process.exit(1)})\""
  }
}
