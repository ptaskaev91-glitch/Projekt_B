{
  "name": "map-k-vercel-mirror",
  "private": true,
  "version": "2.1.1",
  "scripts": {
    "build": "cd map-k && npm ci && VITE_BASE_PATH=/ npm run build && rm -rf ../dist && cp -R dist ../dist"
  }
}
