{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'Root install skipped'",
  "buildCommand": "cd map-k && npm ci && VITE_BASE_PATH=/ npm run build && rm -rf ../dist && cp -R dist ../dist",
  "outputDirectory": "dist"
}
